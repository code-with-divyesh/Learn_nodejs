<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="main-w-full min-h-screen bg-zinc-900">
      <div class="form p-10">
        <form action="/create" method="post">
          <input
            class="block text-white w-full mt-2 outline-none rounded-md px-3 py-2 bg-zinc-800"
            type="text"
            name="title"
            id=""
            placeholder="Title goes here.."
          />
          <textarea
            name="details"
            class="block text-white w-full mt-2 rounded-md outline-none px-3 py-2 bg-zinc-800 resize-none"
            placeholder="Write your task details Here..."
          ></textarea>
          <input
            class="text-white bg-blue-800 px-5 py-2 mt-2"
            type="submit"
            value="Create task"
          />
        </form>
      </div>
      <div class="tasks p-10 flex gap-3 flex-wrap">
        <% if (files.length > 0) { %> <% files.forEach(function(val) { %>
        <div class="task w-72 rounded-md px-3 py-4 bg-zinc-800 text-white">
          <h1 class="text-white text-3xl tracking-tighter"><%=val%></h1>
          <div class="feature flex justify-between item-center">
            <a href="/files/<%=val%>" class="text-blue-600 inline-block mt-3"
              >Read more</a
            >
            <a href="/edit/<%=val%>" class="text-red-600 inline-block mt-3"
              >Edit filename</a
            >
          </div>
        </div>
        <% }); %> <% } else { %>
        <h3 class="text-white bg-zinc-600 px-4 py-2 rounded-md">
          No tasks yet..
        </h3>
        <% } %>
      </div>
    </div>
  </body>
</html>
